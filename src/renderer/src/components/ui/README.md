# UI components

This folder is configured as the `ui` alias in `components.json`, so shadcn/ui components can be added here when the design system is expanded. The small Phase 1 primitives are intentionally compatible with that structure.
